/// Smart JSON parsing utilities for Dart and Flutter.
///
/// This library exposes the `SmartJson` wrapper as the single public entry
/// point. All parsing logic and helpers remain inside the `src/` folder.
library smart_json_parser;

export 'src/smart_json.dart';
